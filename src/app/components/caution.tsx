export default function Caution() {
  return (
    <div className="text-sm text-slate-500 text-center">
      아래 제공되는 API 문서의 데이터 타입은 모두 JSON을 따르며, 가능한 타입은
      아래와 같습니다.
      <br />
      string, number, boolean, null, object, array
    </div>
  );
}
